---
- name: Bootstrap access and ansible user
  hosts: node

  gather_facts: true

  roles:
    - role: ssh_keys_local
    - role: common
    - role: bootstrap_access
    - role: users_ansible
    - role: sshd_hardening

