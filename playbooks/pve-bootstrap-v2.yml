---
- name: Bootstrap access and ansible user
  hosts: pve
  become: true
  become_method: su
  become_user: root

  gather_facts: true

  roles:
    - role: ssh_key_setup_v1
    - role: user_setup_v1
    - role: wg_v1
    
  